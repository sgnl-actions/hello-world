{
  "name": "@sgnl/job-hello-world",
  "version": "1.0.0",
  "description": "Hello World example for SGNL JavaScript jobs",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "rollup -c",
    "build:watch": "rollup -c -w",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "lint": "eslint src/**/*.mjs tests/**/*.js",
    "lint:fix": "eslint src/**/*.mjs tests/**/*.js --fix",
    "validate": "node scripts/validate-metadata.js",
    "dev": "node scripts/dev-runner.js"
  },
  "keywords": [
    "sgnl",
    "job",
    "template",
    "identity",
    "security"
  ],
  "author": "SGNL Engineering",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^24.0.0",
    "@rollup/plugin-node-resolve": "^16.0.1",
    "eslint": "^8.0.0",
    "jest": "^30.0.5",
    "rollup": "^3.0.0"
  }
}